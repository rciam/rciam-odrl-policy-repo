# Auto detect text files and perform LF normalization
* text=auto

# Custom for scripts - ensure they always use LF (Linux/Mac style)
# This prevents "command not found" errors if cloned on Windows
*.sh text eol=lf
*.xml text eol=lf
*.yml text eol=lf
*.md text eol=lf

# Binary files
*.jar binary
*.png binary
*.jpg binary
